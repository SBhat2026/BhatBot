node_modules/
__pycache__/
*.pyc
.DS_Store
.env
.env.local
.env.*.local
*.log
dist/
build/
.cache/
~
.electron-gyp/
OmniParser/

# Phase 3 — learned depth model artifact (lives in ~/.bhatbot, never committed)
depth-model.json
# T5 — learned spoken-length model + dataset (live in ~/.bhatbot, never committed)
spoken-model.json
spoken.jsonl
.episodic-vec.json
.phase4-backup/
