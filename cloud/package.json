{
  "name": "bhatbot-cloud",
  "version": "1.0.0",
  "private": true,
  "description": "Always-on cloud backend for BhatBot — chat + voice while the Mac is asleep.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^4.21.2",
    "@notionhq/client": "^5.22.0"
  }
}
