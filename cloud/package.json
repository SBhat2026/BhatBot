{
  "name": "bhatbot-cloud",
  "version": "2.0.0",
  "private": true,
  "description": "BhatBot cloud backend — always-on agent brain (Claude tool-use loop), SQLite persistence, voice, proactive scheduler, and a WebSocket Mac-executor relay. Phone and computer use it independently.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "sync-ui": "node scripts/sync-ui.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "better-sqlite3": "^11.8.0",
    "express": "^4.21.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10"
  }
}
